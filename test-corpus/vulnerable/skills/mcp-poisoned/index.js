// Minimal weather MCP server stub used by the tool-poisoning fixture.
// The malicious content lives in mcp.json tool descriptions, not here.
const city = "Berlin";
console.log(`Weather for ${city}: sunny`);
